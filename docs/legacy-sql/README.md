# Legacy SQL

These are historical, one-off SQL scripts kept for reference only — they document how the schema evolved but are not applied automatically.

The current schema's source of truth is `supabase/migrations/` only.

Do not execute any file in this directory directly against a live database; if a change is needed, write a proper migration under `supabase/migrations/` instead.
